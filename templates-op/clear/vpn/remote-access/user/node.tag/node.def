help: Specify user name
allowed: local -a a1;
         a1=( /opt/vyatta/etc/ravpn/sessions/*@ppp* );
         local -a a2;
         a2=( "${a1[@]%%@*}" );
         echo -n ${a2[@]##*/};
run: sudo /opt/vyatta/sbin/vyatta-kick-ravpn.pl "$5"
